language: cpp
dist: bionic

install:
  - pyenv global 3.7
  - pip install conan
  - conan user
  - sudo apt-get -y install libsdl2-dev libsdl2-gfx-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-net-dev libsdl2-ttf-dev
  - conan create . user/channel

script:
  - ./configure
  - make
